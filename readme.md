# Example: Ember host app with react mfe

## Usage

## Install and start host app packages

```
cd host-app
npm install
npm start

```

## Install and start mfe app

```
cd react-mfe
pnpm install
pnpm build:preview
```